import discreteSeries  from './src/discreteSeries';
import discreteView  from './src/discreteView';

var discrete = {
    render : discreteView,
    getConfig : discreteSeries,
};

export default discrete; 